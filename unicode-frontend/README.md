# UniCode Academy — Frontend
React 19 + TypeScript + Vite frontend for UniCode Academy.

## Setup
cp .env.example .env
npm install
npm run dev

## Build
npm run build

## Environment variables
VITE_API_URL=http://localhost:8080
VITE_GOOGLE_CLIENT_ID=   (optional)
VITE_WS_URL=ws://localhost:8080
